services:
  - type: web
    name: google-map-scoop-api
    env: node
    buildCommand: echo "Skipping build - server only"
    startCommand: npm run server
    envVars:
      - key: NODE_ENV
        value: production